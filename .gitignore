# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# IDE
.idea/
*.iml
.vscode/

# Google Play upload signing — never commit these
android/key.properties
android/upload-keystore.jks
android/*.jks
android/*.keystore
android/keystore-credentials.txt

# Generated by the build scripts in ../scripts. Large, and reproducible with
# `npm run flutter:assets` from the repo root — but see SETUP.md before
# deciding to commit them anyway.
assets/bible.db
assets/wonders.json

# Derived from branding/app-icon.png by tool/make_branding.dart. The source
# picture beside them is the one to keep.
branding/adaptive-foreground.png
branding/splash-icon.png
